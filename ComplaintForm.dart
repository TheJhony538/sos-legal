import 'package:flutter/material.dart';

class ComplaintForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Formulario de Quejas')),
      body: Center(child: Text('Pantalla del Formulario de Quejas')),
    );
  }
}